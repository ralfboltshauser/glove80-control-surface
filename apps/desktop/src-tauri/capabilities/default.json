{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the main editor window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
